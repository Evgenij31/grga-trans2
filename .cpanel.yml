---
deployment:
  tasks:
    - export DeploymentPath=/home/webevermk/public_html/test.grga.mk
    - /usr/bin/npm install
    - /usr/bin/npm run build
    - /bin/cp -R dist/* $DeploymentPath/
    - /bin/rm -f $DeploymentPath/.cpanel.yml
